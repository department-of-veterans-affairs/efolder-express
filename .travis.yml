language: python
python:
    - 2.7
    - pypy

cache:
    directories:
        - $HOME/.cache/pip


install:
    - pip install -U pip
    - pip install -r requirements.txt -r test-requirements.txt flake8 flake8-import-order

script:
    - py.test
    - flake8
